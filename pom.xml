<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>org.kbalazs</groupId>
    <artifactId>smart_scrum_poker_backend_native</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>smart_scrum_poker_backend_native</name>
    <description>smart_scrum_poker_backend_native</description>
    <packaging>pom</packaging>

    <modules>
        <module>db-module</module>
        <module>app-module</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <postgresql.version>42.7.3</postgresql.version>
        <jooq.version>3.19.21</jooq.version>
        <flyway.version>10.10.0</flyway.version>
        <mapstruct.version>1.6.3</mapstruct.version>
        <rita.version>2.4.501</rita.version>
        <lombok.version>1.18.32</lombok.version>
        <spring.version>6.1.8</spring.version>
        <spring.security.version>6.2.2</spring.security.version>
        <spring.security.messaging.version>7.0.4</spring.security.messaging.version>
        <httpclient5.version>5.3.1</httpclient5.version>
        <mustache.version>0.9.13</mustache.version>
        <logstash.version>7.4</logstash.version>
        <spring.boot.oauth2.authorization.server.version>3.5.12</spring.boot.oauth2.authorization.server.version>
        <hikaricp.version>5.1.0</hikaricp.version>
        <maven.surefire.plugin.version>3.2.5</maven.surefire.plugin.version>
        <lombok.maven.plugin.version>1.18.20.0</lombok.maven.plugin.version>
        <jaxb.version>3.0.1</jaxb.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>3.3.0</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

</project>
