name: Review Command

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  issue_comment:
  pull_request_review_comment:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Hello
        run: echo "hello"